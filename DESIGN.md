# 设计决策记录

记录每个决策"为什么这么选"，以及被放弃的方案。

## 1. 用原生 tool calling，不用文本协议（ReAct / XML）

模型原生的 function calling 由服务端保证结构，参数直接是 JSON，省掉自己写正则
解析器的一大摊事，也避免模型把工具调用写在自然语言里导致漏解析。

代价是绑定了支持 tool calling 的模型。`llm.py` 把这层隔离在一个模块里，
将来要换成文本协议只动这一处。

## 2. 工具 Schema 手写，不从函数签名反射生成

工具描述的措辞是影响 agent 表现最大的变量之一——`read_file` 的描述里多写一句
"修改文件前必须先读取"，模型的行为就明显不同。手写 schema 才能逐字调优。
反射方案把描述质量绑死在 docstring 格式上，收益不划算。

## 3. `dispatch()` 永不抛异常

工具不存在、参数不是合法 JSON、参数对不上、执行时崩了——所有失败都被翻译成
一段中文错误文本，作为 tool 消息回灌给模型。

理由：agent 的鲁棒性来源是"模型能看见错误并自己纠正"，而不是"程序不出错"。
模型看到"文件不存在：src/mian.py"能自己发现拼写错了；如果异常直接抛到顶层，
整轮任务就断了，用户还得重新描述一遍需求。

## 4. 三层循环终止条件

1. **模型返回的消息不含 tool_calls** —— 正常出口。模型认为话说完了，
   控制权交还用户。这是唯一的"任务完成"信号，不额外设 `finish` 工具：
   多一个工具就多一种模型可能忘记调用的东西。
2. **步数上限 `max_steps`** —— 防止"工具持续失败 → 模型持续重试"的死循环。
   这种循环不会自己停，而且每一步都在烧钱。
3. **Ctrl+C** —— 中断当前轮但保留历史，下一轮可以接着聊。

## 5. 路径沙箱放在工具层，而不是提示词里

提示词是建议，代码才是约束。模型可能读到文件内容里的诱导性指令，也可能自己
拼错路径。`tools/paths.py` 的 `resolve()` 先 `.resolve()` 消解 `..` 和符号链接，
再检查结果是否落在工作目录内，任何文件工具都必须先过这一关。

## 6. 流式 tool_call 必须按 index 累加

流式响应里，一次工具调用是被切碎下发的：`id` 和 `name` 通常只在第一个分片出现，
`arguments` 会被切成很多段陆续送达。所以要按 `index` 建槽位做字符串累加，
不能假设"收到一个 tool_calls 分片 = 一次完整调用"。

同时：一条 assistant 消息里带了几个 tool_call，后面就必须跟几条 `tool_call_id`
对应的 tool 消息，缺一条整轮请求都会被服务端拒绝——所以工具即使执行失败，
也一定要回灌一条结果。

## 7. 工具调用串行执行

模型一次给出的多个调用之间可能有写-读依赖（先写文件再读回来验证），
并行会引入顺序不确定性。等后面把工具区分出只读/写入两类，再对只读工具开并发。

## 8. 超长工具结果保留头尾

中间部分通常是重复内容（大量日志、长文件正文），而头尾包含命令回显和最终报错，
是模型最需要的信息。直接砍尾部会丢掉最关键的 traceback。

## 9. 显式指定 UTF-8 读写文件

Python 在中文 Windows 上 `open()` 的默认编码是 GBK，读 UTF-8 源码会直接抛
`UnicodeDecodeError`。所有文件读写都显式传 `encoding="utf-8"`。

## 10. `edit_file` 用唯一字符串替换，不用行号定位

行号会因为上下文陈旧（模型是几步之前读的文件，中间可能被别的调用改过）而
错位；整段重写又太浪费输出 token、容易在长文件里漏掉不该动的内容。
唯一字符串匹配失败时（找不到 / 不唯一）直接把原因说清楚让模型重新
`read_file`，这比"改错地方"更安全——错误是可见可恢复的，静默改错不是。

## 11. 三档权限模式 + 确认闸门放在工具 handler 内部，不放在 dispatch() 里统一拦

`write_file` / `edit_file` / `run_command` 各自知道"要不要确认"该给用户看
什么（diff 还是命令原文），所以由它们自己在执行前调用
`ctx.permissions.request()`，而不是在 `tools/base.py::dispatch()` 里
用一个通用的 `requires_approval` 布尔位统一弹一个内容空洞的确认框。
`requires_approval` 这个字段目前仅用于工具自描述（未来做只读/写入的
并发划分时会用到），实际的闸门调用权在各工具自己手上。

三档模式：
- `ask`（默认）—— 每次破坏性操作都问，用户可以选"这个工具本次会话总是允许"；
- `auto`—— 全自动，跳过确认，但 `run_command` 的硬性黑名单仍然生效——
  auto 不等于无底线；
- `readonly`—— 直接拒绝任何写操作，返回值会告诉模型"现在只能分析，别再试"。

没有交互通道时（`Permissions.asker is None`）一律拒绝（fail closed），
不会因为忘记接 UI 就悄悄放行破坏性操作。

## 12. `run_command` 的高危命令用正则黑名单硬拦截，不受权限模式影响

`auto` 模式是为了演示流畅，不是为了放弃底线：`rm -rf /`、`git push --force`、
`git reset --hard`、格式化磁盘之类命令即使在 `auto` 模式下也会被直接拒绝，
不会触达用户确认这一步——这类操作没有"确认后执行"的价值，唯一正确的处理
就是拒绝并让模型换一种做法。黑名单不追求穷尽（不可能穷尽），只挡最常见、
最难挽回的几类，属于"防灾难性误操作"而不是"防恶意注入"。

## 13. Windows 上显式起 `powershell.exe`，不能依赖 `shell=True`

写这个工具时踩了一个坑：`subprocess.run(cmd, shell=True)` 在 Windows 上
调用的是 `%COMSPEC%`，也就是 `cmd.exe`，不是 PowerShell。如果工具描述里
告诉模型"这里能用 PowerShell"，模型写出的 `$PSVersionTable`、`Get-ChildItem`
之类语法在 cmd.exe 下全部报错，而且报错信息（"不是内部或外部命令"）
不会让模型意识到问题出在 shell 选错了，只会反复重试同样会失败的写法。
现在 Windows 下改为显式拼 `["powershell", "-NoProfile", "-NonInteractive",
"-Command", command]` 并关闭 `shell=True`；其它平台仍用 `shell=True`
默认调用的 `/bin/sh`。工具描述的措辞必须和实际行为对齐，这类"文档说的和
代码做的不一样"的偏差在纯文本协议里很难被发现，因为它不会报 Python 异常，
只会让 agent 在特定平台上莫名其妙地反复失败。

## 14. 上下文压缩：难点在"从哪里切"，不在"怎么摘要"

写之前以为主要工作量是设计摘要提示词，实际做下来发现真正的坑是切割点。

OpenAI 兼容协议有一条硬约束：每条 `role="tool"` 的消息，都必须能在它前面
找到一条带有对应 `tool_call_id` 的 assistant 消息。如果压缩时从中间随便切
一刀，把 assistant 留在被摘要掉的那半边、tool 结果留在保留的这半边，整个
请求会被服务端直接以 400 拒绝——而且报错信息完全不会提示你是压缩逻辑切坏了，
排查起来非常费劲。

所以 `_find_safe_cut()` 的规则是：切割点之后的第一条消息绝不能是
`role="tool"`。优先切在 user 消息处（一轮任务的自然边界，语义最完整），
找不到就退而求其次切在 assistant 消息处；如果整段尾巴都是 tool 消息，
宁可放弃这次压缩也不切坏（`compact()` 返回 `compacted=False` 并说明原因）。

已针对这一点写了离线测试覆盖：多轮完整对话、切割点恰好落在 tool 消息中间、
尾部全是 tool 消息、连续压缩两次（摘要本身被再次摘要）等场景，
逐个校验压缩后的消息序列仍满足 tool_call 配对约束。

## 15. token 计量用 API 返回的 prompt_tokens，不本地跑分词器

DeepSeek 没有公开可离线使用的 tokenizer，硬套 tiktoken 在中文上误差能到
30% 以上。而每次响应的 `usage.prompt_tokens` 正好就是"刚刚发出去的这份历史
有多大"的权威答案——白给的精确值，没有理由不用。

只有"上次实测之后新追加的内容"需要本地估算，按 2.5 字符/token 一刀切
（中文约 1、英文约 4，取中间值）。这个估算只用于"要不要触发压缩"的二元判断，
且阈值本身留了足够余量，不需要更高精度。

压缩后要把实测值清零重估：它对应的是压缩前的历史，不清掉的话下一轮会拿着
旧的大数字立刻再次触发压缩。

## 16. 摘要用 user 角色注入，摘要请求不带 tools

**角色选择**：assistant 角色会让模型误以为这些话是它自己说过的；在对话中段
插入第二条 system 消息，各家兼容实现的行为并不一致。user 角色是最稳妥、
各家都支持的"外部信息注入"方式，配合明确的方括号标记说明这是摘要。

**不带 tools**：摘要请求如果带上 tools 参数，模型很可能"接着干活"直接发起
新的工具调用，而不是老老实实写摘要。这也是为什么摘要请求把对话平铺成纯文本
再传入，而不是把原始 messages 结构原样传回去。

**摘要提示词面向未来而非叙事**：摘要的唯一读者是即将接着干活的模型自己，
所以按"用户目标 / 已完成的工作 / 关键事实 / 未完成待办"结构化输出，
要求保留确切的文件路径和标识符名称，而不是复述对话过程。

**失败不能拖垮任务**：摘要调用本身也可能超时或报错，`compact()` 把它包在
try 里，失败就保持历史原样返回，不让一次压缩失败导致整轮任务挂掉。

## 17. 压缩开销单独记账

摘要本身要调一次模型，是有成本的。把它混进 `TurnStats.tokens` 会让人看不出
压缩的真实代价，所以单独用 `compaction_tokens` 统计并在终端分开显示。

实测中这个数字很有意义：把阈值强行调到 5000 做测试时，一轮任务干活花了
9578 tokens，压缩却额外花了 5051 tokens——压缩比省下的还贵。这说明阈值
不能设太低，默认的 40000 是在"避免撞上下文上限"和"别为压缩本身付太多钱"
之间取的平衡。

## 18. Web 界面：Reporter 抽象的兑现

加浏览器界面时 `agent/` 包**一行代码都没改**，只是新写了一个 `WebReporter`
实现同一个 `Reporter` 接口。当初把 rich 全部关在 `cli.py` 里、让 `loop.py`
只依赖抽象接口，回报就在这里。

界面本身刻意不上 React/Vue 工程化：agent 必须跑在 Python 进程里（要读写本地
文件、执行命令，浏览器碰不了），所以浏览器只能是显示层，页面上统共 5 个元素。
npm 那套买的是组件化和打包，这个规模换不回成本，反而给仓库塞进 node_modules、
让评委多一步 `npm install`、多一个构建步骤可能在截止前出问题。
单个 HTML 文件 + 原生 JS，不存在"构建失败"这种状态。

## 19. Web 端的两个线程模型问题

**问题一：`Agent.run_turn()` 是同步阻塞的。** 直接在事件循环里跑，整轮任务
期间 WebSocket 收不到任何消息——包括用户点"允许"的那条，确认框会永久卡死，
形成死锁。所以 agent 跑在独立线程里。

**问题二：`ask_approval()` 要在 agent 线程里阻塞等待浏览器的回答。**
用 `threading.Event` 桥接：Reporter 发出 `approval_request` 事件后
`wait()`，WebSocket 接收侧拿到回答就 `set()` 唤醒。

这里有两个必须补的口子，都已处理：
- `wait()` 必须带超时。否则用户直接关掉页面，agent 线程会永久挂死。
  超时按拒绝处理，与权限系统 fail-closed 的原则一致。
- WebSocket 断开时（`finally` 块）主动以"拒绝"唤醒可能还卡着的线程。
  已实测：确认框弹出时粗暴断开连接，文件不会被写出，服务端仍能接受新连接。

**事件跨线程投递**用 `loop.call_soon_threadsafe()`，不能在工作线程里直接
`await ws.send_json()`。

## 20. 上下文占用进度条

上下文压缩是这个项目技术含量最高的部分，但它平时完全不可见。界面上按
`history.estimated_tokens() / compact_threshold` 画一条进度条后，压缩触发的
瞬间它会肉眼可见地回落——把一个纯后台机制变成了可以直接指着讲的东西。

## 21. 凭据类文件一律拒绝访问，即使它在工作目录内

加 Web 文件预览时发现的一个真问题：路径沙箱只管"有没有越出工作目录"，
而 `.env` 就躺在工作目录里。也就是说 agent 的 `read_file` 完全可以读出
自己的 API key 并原样打印进对话——而对话会出现在终端录屏、Web 界面里。
题目明确要求密钥不得出现在仓库、README 或视频中，这就是个实打实的雷。

所以在 `paths.py` 里加了第二道检查：`.env`、`*.key`、`*.pem`、`id_rsa*`、
`.git-credentials` 等模式一律拒绝读写，`.env.example` 这类模板文件放行
（不含真实凭据，本来就要入库）。

有一处容易漏：`grep` 是直接遍历文件读内容的，**没有走 `resolve()`**，
所以必须在它的文件迭代器里单独再挡一次——否则一句 `grep sk-` 就能把
key 捞出来。已验证：拿真实密钥的前缀去 grep 也搜不到任何东西。

这道检查放在工具层，所以终端界面和 Web 界面同时受益，Web 的
`/api/file` 复用同一个 `resolve()`，不需要各写一份。

## 22. 两类文件接口的边界不同，这是刻意的

Web 端有两组接口，安全模型故意不一样：

- `/api/tree`、`/api/file` —— **走 agent 的同一套路径沙箱**，只能看当前
  工作目录内的东西，且同样拒绝凭据文件。
- `/api/browse` —— **刻意不受沙箱约束**。要在界面上选一个新的工作目录，
  就必须能看到当前工作目录外面，这是功能本身的要求，绕不开。

给 `/api/browse` 的补偿是：它只返回目录名，绝不返回任何文件内容；服务只
监听 127.0.0.1。关键是沙箱本身没有被削弱——agent 依然只能读写"选定的"
那个工作目录，浏览目录的能力属于人，不属于 agent。

## 23. 切换工作目录要重建 Agent，不能只改 workspace 字段

系统提示词里带着启动时的目录概览，对话历史也全是围绕旧目录展开的。
只换掉 `ToolContext.workspace` 而留着历史，模型会拿着旧目录的印象在新目录
里干活，必然出错。所以 `set_workspace` 直接重建 Agent 并重置历史，
界面上也明确提示"对话历史已重置"。

另外两个细节：任务进行中不允许切换（`busy()` 拦住）；切换失败时回滚到
原来的工作目录并重建，不留下半损坏的状态。

## 24. 会话存档不保存 system prompt

系统提示词里嵌着启动那一刻的目录概览。上次会话结束后文件可能已经增删，
把旧快照原样恢复回来，模型会拿着过期的目录印象干活，比没有记忆更糟——
它会"确信"某个文件存在。所以存档只存 user/assistant/tool 三类消息，
system prompt 每次按当前目录现场重建。

历史里的对话内容照旧保留：它记录的是"做过什么"，这个事实不会过期。

另外两个细节：
- **按工作目录分别存档**。同一个 agent 会在不同项目里用，共用一份历史只会
  互相污染。存档名用「目录名 + 绝对路径哈希」，既可读又不会因重名撞车。
- **原子写入**。每轮任务结束都落盘，而落盘时被 Ctrl+C 打断完全可能。
  先写临时文件再 `os.replace`，保证存档要么是上一个完整版本、要么是这一个，
  不会出现写了一半的坏 JSON。

## 25. 存档覆盖必须提示，不能静默发生

联调时踩到的坑：做完一个长任务，随手在同一目录跑了个一次性命令（没加
`--resume`），上次的历史就被这一轮的存档覆盖没了，而且毫无提示。

现在不带 `--resume` 启动且检测到已有存档时会明确告知："该目录有历史会话
（N 轮），本次将开新对话并在结束时覆盖它；要接着上次请加 --resume。"
把一个静默的数据丢失变成一次知情选择。

`run_turn` 的存档调用放在 `finally` 里，三个出口（正常结束 / 步数上限 /
中断报错）都会落盘——否则 Ctrl+C 一次就丢掉整轮进度。

## 26. todo_write：为什么值得多加一个工具

多步任务里模型很容易"做着做着忘了还有什么没做"，尤其是中途被工具报错带偏
之后。把计划显式写进对话历史，等于给模型一个随时可查的锚点；对用户来说，
也终于能看见 agent 打算干什么、干到哪一步。

代价是多一个工具就多一种模型可能用错的地方，所以描述里明确写了"三步以上
才用"，避免它给「读个文件」这种一步任务也列个清单。

**状态机刻意约束成「同时最多一项 in_progress」**：允许多项并行的话，模型会
把所有条目一次性标成进行中，清单就退化成一份静态列表，失去了跟踪进度的意义。
违反时直接报错让它改，而不是默默接受。

UI 上做了两个针对性处理（都是实测后才发现有必要的）：
- **不给 todo_write 画工具调用卡片**。清单本身就是可视化，再显示一遍带长
  JSON 参数的调用卡片纯属刷屏，信息完全重复。
- **清单卡片每次更新都挪到消息流末尾**。它表示"当前进度"，留在原地会被
  后续消息滚走，那就失去意义了。

## 27. 路径沙箱管不住 run_command，这是设计边界不是疏漏

演练演示样例时实测到的：让 agent 读工作目录外的 `C:/Windows/win.ini`，
`read_file` 被路径沙箱正确拒绝，但模型紧接着改用
`run_command` 执行 `Get-Content C:/Windows/win.ini`，**读到了**。

这不是 bug，是能力边界，必须说清楚：

- **路径沙箱的适用范围是文件工具**（read_file / write_file / edit_file /
  grep / list_dir）。它防的是模型拼错路径、或被文件内容里的诱导性指令带偏，
  这类"误操作"占实际风险的绝大多数。
- **run_command 无法被同样地约束**。shell 能做的事没有边界——`cd ..`、
  管道、重定向、调用另一个解释器，靠字符串匹配去拦是拦不住的。真要隔离，
  需要容器或操作系统级的权限控制，这超出了本项目的范围。

所以 run_command 的防线不是沙箱，而是**人工确认**：ask 模式下每条命令都要
用户点头，且确认框展示命令原文。实测的完整链路正好体现了纵深防御——
沙箱挡住文件工具 → 模型试图改用命令绕道 → 确认框弹出并展示
`Get-Content C:\Windows\win.ini` → 用户拒绝 → 模型收到"不要重复同样的
调用"的提示。

**auto 模式因此是一次明确的信任让渡**：跳过确认就等于放弃了这层防线，
此时只剩高危命令黑名单兜底。这也是为什么默认模式是 ask 而不是 auto。

## 28. 怎么验证压缩没丢信息：三层，前两层可证明，第三层只能测量

"压缩是否正确"其实是三个问题，可验证程度完全不同，混为一谈就答不清楚：

**第一层 结构正确性** —— 压缩后的历史仍是合法请求体：没有孤儿 tool 消息、
每个 tool_call 都有结果、system prompt 没丢。这是确定性的，可以证明，
由 `tests/test_history.py` 覆盖（含"切割点恰好落在 tool 块中间""整段尾巴
都是 tool 消息""连续压缩两次"等边界）。

**第二层 机械正确性** —— 最近若干条消息原样保留、摘要非空、token 确实下降、
摘要生成失败时历史保持原样。同样是确定性的，同一批测试覆盖。

**第三层 语义保真度** —— 摘要有没有保住"继续干活所必需的事实"。
摘要是模型生成的，**无法证明无损**，只能测量。承认这一点比假装能证明更诚实。

测量方法是探针测试（`tests/eval_compaction.py`）：在对话里埋下若干可检验的
事实——一部分来自用户消息，一部分来自工具返回的内容——把阈值压到 6000
强制触发压缩，然后**禁用全部工具**提问。

禁用工具这一步是关键：留着工具的话，模型答不上来时会去重新读一遍文件，
测出来的就不是"压缩保住了多少"，而是"模型会不会补救"，指标就废了。

实测跑两次均为 6/6 全中，被考的事实包含压缩前埋入的项目代号与人名
（确认落在被摘要掉的那一半里，不是靠 keep_recent 窗口蒙混过关）。
脚本在召回率低于 80% 时返回非零退出码，可以直接当门禁用。

它会真实调用 API 消耗额度，所以刻意不放进 `run_all.py`。

## 29. web_fetch：为什么只做抓取不做搜索，以及它的三条硬边界

加联网能力时第一个要回答的问题是做哪一半。**fetch 和 search 是两件事**：
fetch 是"读一个已知地址"，一次 HTTP GET 加正文抽取，标准库就能做完；
search 是"把一句自然语言变成一组地址"，需要全网倒排索引——自己造不出来，
只能租第三方（Serper / Brave / Bing），要 API key、按次付费、多一份凭据要管。

选择只做 fetch，理由是**大量站点自带站内检索**，抓它们的搜索页就够了。
实测洛谷：`problem/list?keyword=采药` 返回的结果列表就嵌在页面里，
从题目名找到题号只需要一次 fetch，不需要搜索引擎。真正非 search 不可的
场景是"我不知道该去哪个站"，那是另一个量级的投入，不在本项目范围内。

### 三条边界写在代码里，不指望提示词

1. **只允许 http/https。** `file://` 能读任意本地文件，等于把辛苦做的
   路径沙箱整个绕过去。
2. **拒绝内网与回环地址，且重定向逐跳校验。** 不做这条，模型可以抓
   `127.0.0.1:8000`（Web 界面自己）或 `169.254.169.254`（云厂商元数据
   接口）。这类 SSRF 的要害在于请求是从这台机器内部发出的，防火墙拦不住。
   校验必须落在解析出的 IP 上而不是字面量：`localtest.me` 是个公网域名，
   但解析到 127.0.0.1，只做字符串匹配挡不住。只校验最终落点同样不够——
   公网地址可以 302 到内网，那一跳的请求照样会真的发出去。
3. **只接受文本类 Content-Type，下载有字节上限。** 二进制塞进上下文毫无意义。

### 刻意不要求用户确认，代价要说清楚

GET 是只读的，本地什么都不改，而查资料往往要连着抓好几页，每页弹一次
确认框这工具就没法用了。但要如实承认代价：**URL 本身是一条出网通道**，
auto 模式下模型可以把任意内容拼进 query string 发出去。真要堵这个口子
得做域名白名单，那是另一个层级的工程。这和第 27 条是同一类判断——
把边界写清楚，比假装没有边界更有价值。

### 两个实现上非做不可、但事先没想到的点

**必须收 cookie。** 洛谷首次访问会先下发 cookie 再 302，客户端不收 cookie
就在同一个地址上反复跳转，urllib 最后报的是"无限重定向"——一个完全不指向
真实原因的错误。每次调用用一个全新的 cookie 罐、用完即弃：不跨调用复用，
否则 cookie 会累积成会话状态，一次登录态可能被带到无关站点上去。

**必须保留 type="application/json" 的 script 块。** 一开始把所有
`<script>` 一刀切掉，结果是洛谷搜索页变成一片空白。这类标签装的是数据不是
代码（JSON-LD 就是这么规定的），站点普遍用它把页面数据内联进 HTML，而且
**往往比渲染出来的 HTML 更完整**——洛谷题目页的输入输出样例和时空限制
只存在于这里，页面上那几个小节反而没有。这一点推翻了我原本的判断：
先看渲染结果就以为"样例抓不到"，其实只是没往数据块里看。保留时顺手把
Unicode 转义解开，原始 JSON 里一个汉字占 6 个字符，P1048 那一页因此
从 15243 字符降到 8640。

正文截断沿用 `history._truncate` 的头尾保留策略，而不是只留头部：洛谷把
服务端渲染的正文放在页面最末尾，只留头部会正好把要读的内容切掉。

另外一个小坑：模型写出来的 URL 十有八九直接带中文（`?keyword=采药`），
urllib 只收 ASCII，不做百分号编码就是一个 UnicodeEncodeError。编码时
`safe` 里要带上 `%`，否则已经编码过的地址会被二次编码成 `%25E9`。

## 待办 / 待权衡

- 压缩目前是"整段摘要"。另一种思路是分级处理：老的 tool result 直接丢弃或
  截断得更狠、但保留 user/assistant 的对话原文。前者实现简单、压缩率高，
  后者保真度更好。当前选前者，因为工具结果往往是最占体积也最容易过期的部分。
