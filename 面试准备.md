# mini coding agent · 面试准备手册

> 这份文档回答两个问题：**做了什么** 和 **为什么这么做**。
> 题目原文说面试提问重点是"你是否理解你的 agent 为什么这样运转，是否能为你的设计决策给出辩护"，
> 所以每一节都按「实现了什么 → 怎么实现 → 为什么这么选 → 放弃了什么方案」组织。
>
> 配套材料：`DESIGN.md`（28 条决策记录，本文的加长版）、`demo/演示样例.md`（录制脚本）。

---

## 零、30 秒电梯陈述

> 我从零实现了一个编程智能体。它接一句自然语言指令，自己决定读哪些文件、
> 改哪一行、跑什么命令，直到任务完成。核心是一个「请求模型 → 执行工具 → 结果回灌 → 再问模型」
> 的循环，加上七个本地工具、一套三层安全边界，和一套能在上下文写满时把早期对话
> 摘要压缩、且不破坏 tool_call 配对的历史管理机制。终端和浏览器两套界面共用同一个内核。
> 全程没有用任何 agent 框架，`openai` 库只当 HTTP 客户端用。

**规模**：Python 约 3500 行 —— agent 内核约 2000 行 / Web 后端 465 行 / 测试 935 行；37 个离线测试全通过。

---

## 一、架构总览

### 1.1 模块与职责

```
agent/                          内核，不依赖任何 UI
├── cli.py       201 行   终端 REPL + rich 渲染 + 斜杠命令
├── loop.py      215 行   ★ 主循环、Reporter 接口、TurnStats
├── llm.py       160 行   ★ 与模型通信、流式分片拼装、重试
├── history.py   214 行   ★ 对话历史、token 计量、上下文压缩
├── session.py    96 行   会话持久化（--resume）
├── config.py    102 行   配置与 .env 加载
├── prompts.py    87 行   系统提示词 + 摘要提示词
└── tools/                工具层
    ├── base.py       159 行  ★ 注册表、Schema 导出、dispatch 兜错
    ├── paths.py       77 行  ★ 路径沙箱 + 凭据文件拦截
    ├── permissions.py 83 行  ★ 三档权限模式 + 确认闸门
    ├── filesystem.py 153 行  read_file / list_dir
    ├── search.py     103 行  grep
    ├── editing.py    117 行  write_file / edit_file
    ├── shell.py      131 行  run_command + 高危命令黑名单
    ├── planning.py    90 行  todo_write
    └── diffutil.py    30 行  统一 diff 生成

web/server.py    460 行   FastAPI + WebSocket，实现 WebReporter
web/static/index.html     单文件前端，原生 JS，无构建步骤
tests/           935 行   37 个用例，模型全部打桩，不联网
```

### 1.2 依赖方向（面试要能画出来）

```
        ┌───────────────┐        ┌───────────────┐
        │   cli.py      │        │ web/server.py │
        │ConsoleReporter│        │  WebReporter  │
        └───────┬───────┘        └───────┬───────┘
                │  都实现 Reporter 接口   │
                └───────────┬────────────┘
                            ▼
                     ┌─────────────┐
                     │   loop.py   │ ← 只认抽象接口
                     │    Agent    │   不认 rich，不认 FastAPI
                     └──┬───┬───┬──┘
              ┌─────────┘   │   └─────────┐
              ▼             ▼             ▼
        ┌──────────┐  ┌───────────┐  ┌──────────┐
        │  llm.py  │  │history.py │  │  tools/  │
        └──────────┘  └───────────┘  └──────────┘
```

**一句话要点**：依赖箭头永远从 UI 指向内核，从不反向。
这条纪律的回报是可验证的 —— 加浏览器界面时 `agent/` 包**一行代码都没改**。

---

## 二、一次对话的完整生命周期

面试最可能被问的第一个问题就是"讲讲你的 agent 怎么跑起来的"。照这条链讲：

```
用户输入
   │
   ▼
① Agent.run_turn()                                    loop.py:110
   └─ try/finally 包住，finally 里存档 → 三个出口都不丢进度
   │
   ▼
② history.add_user(text)                              history.py:59
   │
   ▼
③ ┌── while stats.steps < max_steps ──────────────────┐  loop.py:122
   │                                                   │
   │  ④ 检查是否需要压缩                                │
   │     history.needs_compaction()          history.py:127
   │     └─ 是 → compact()：把早期对话摘要成一条         │
   │        ※ 必须在发请求「之前」，等 400 回来就晚了     │
   │                                                   │
   │  ⑤ client.complete(messages, tools, on_text)  llm.py:70
   │     ├─ stream=True，边收边把文本 delta 推给 UI      │
   │     ├─ tool_call 按 index 建槽位累加        llm.py:141
   │     └─ 网络错误退避重试，4xx 直接抛         llm.py:90
   │                                                   │
   │  ⑥ 记账 usage：total_tokens / prompt_tokens        │
   │     history.note_prompt_tokens()        history.py:107
   │                                                   │
   │  ⑦ history.add_assistant(message)                 │
   │                                                   │
   │  ⑧ if not message.tool_calls: return ────────────→ 出口 1（正常）
   │                                                   │
   │  ⑨ _run_tools()：串行执行              loop.py:198 │
   │     for call in tool_calls:                       │
   │       ├─ dispatch(name, args, ctx)      base.py:104
   │       │   ├─ 工具不存在   → 错误文本               │
   │       │   ├─ JSON 非法    → 错误文本               │
   │       │   ├─ 参数不匹配   → 错误文本               │
   │       │   └─ 执行异常     → 错误文本  ※ 永不上抛    │
   │       │       └─ 写操作在这里弹确认框 / 查黑名单     │
   │       └─ history.add_tool_result(call.id, content)│
   │          ※ 无论成败都必须回灌，否则下轮请求 400     │
   └───────────────────────────────────────────────────┘
   │
   ▼ 步数耗尽 ─────────────────────────────────────────→ 出口 2（上限）
   Ctrl+C ────────────────────────────────────────────→ 出口 3（中断）
```

---

## 三、核心机制逐个拆解

### 3.1 主循环与三层终止条件 · `loop.py`

**实现**：`while stats.steps < self.config.max_steps`，每轮一次模型调用。

**三个出口，缺一不可**：

| # | 条件 | 位置 | 为什么需要 |
|---|---|---|---|
| 1 | 模型回复不含 `tool_calls` | loop.py:151 | **正常出口**。模型认为话说完了 |
| 2 | 步数达到 `max_steps`（40） | loop.py:122 | 防"工具持续失败 → 模型持续重试"的死循环烧钱 |
| 3 | 用户 Ctrl+C | loop.py:139 | 中断本轮但保留历史，下轮可续 |

**辩护点 ①：为什么不设一个 `finish` / `attempt_completion` 工具？**
> 多一个工具就多一种模型可能忘记调用的东西。"不再请求工具"本身就是最自然、
> 最不会出错的完成信号 —— 它不需要模型额外记住任何约定。

**辩护点 ②：撞上步数上限时为什么不往历史里补东西？**
> 因为 `_run_tools()` 一定会在 while 条件再次检查之前跑完，每个 `tool_call`
> 都已经有了配对的 tool 消息，历史本身是完整的。所以只提示用户，
> 历史原样保留 —— 下一轮用户追一句"继续"就能接着干。

**辩护点 ③：为什么工具串行执行不并行？**
> 模型一次给出的多个调用之间可能有写-读依赖（先写文件再读回来验证），
> 并行会引入顺序不确定性。要做并发，正确的路径是先把工具区分成只读/写入两类，
> 只对只读工具开并发 —— `Tool.requires_approval` 字段已经为此预留。

---

### 3.2 流式响应解析 · `llm.py`

**这是最能体现"自己写了解析"的一段，务必讲透。**

流式响应里，一次工具调用是**被切碎下发**的：

```
chunk 1: tool_calls[0] = {index:0, id:"call_abc", function:{name:"read_file", arguments:""}}
chunk 2: tool_calls[0] = {index:0, function:{arguments:"{\"pa"}}
chunk 3: tool_calls[0] = {index:0, function:{arguments:"th\": \"ma"}}
chunk 4: tool_calls[0] = {index:0, function:{arguments:"in.py\"}"}}
```

**实现**（llm.py:141）：按 `index` 建槽位字典，`id`/`name` 取第一次出现的值，
`arguments` 做字符串累加，流结束后按 index 排序组装成 `ToolCall` 列表。

```python
slot = slots.setdefault(fragment.index, {"id": "", "name": "", "arguments": ""})
if fragment.id:                 slot["id"] = fragment.id
if fragment.function.name:      slot["name"] += fragment.function.name
if fragment.function.arguments: slot["arguments"] += fragment.function.arguments
```

**踩过的坑**：不能假设"收到一个 tool_calls 分片 = 一次完整调用"。
按分片直接建对象的话，一次调用会被拆成四个残缺的调用，全部解析失败。

**另一条硬约束**（llm.py:42 注释）：一条 assistant 消息里带了 N 个 `tool_call`，
后面就**必须**跟 N 条对应 `tool_call_id` 的 tool 消息，缺一条整轮请求就被服务端拒绝。
这条约束贯穿全项目 —— 工具执行失败也要回灌、压缩不能切断配对、存档恢复要保持配对。

**重试策略**（llm.py:79）：

| 错误类型 | 处理 |
|---|---|
| `APIConnectionError` / `APITimeoutError` / `RateLimitError` | 指数退避重试（1、2、4 秒），最多 3 次 |
| `5xx` | 同样重试 |
| `4xx` | **立即抛出**，重试没有意义（请求本身就是错的） |

刻意设 `OpenAI(max_retries=0)` 关掉库自带的重试，自己控制，便于区分错误类型和打日志。

---

### 3.3 工具系统 · `tools/base.py`

**注册方式**：装饰器登记进全局 `REGISTRY`。

```python
@tool(name="read_file", description="...", parameters={...})
def read_file(ctx, path, offset=1, limit=2000) -> str: ...
```

`get_schemas()` 把 REGISTRY 按 OpenAI tools 格式导出，直接塞进请求体。

**辩护点 ①：Schema 为什么手写，不从函数签名反射生成？**
> 工具描述的措辞是影响 agent 表现最大的变量之一 —— `read_file` 的描述里
> 多写一句"修改文件前必须先读取"，模型行为就明显不同。手写才能逐字调优。
> 反射方案把描述质量绑死在 docstring 格式上，收益不划算。

**辩护点 ②（最重要）：`dispatch()` 永不向上抛异常。**

四类失败全部翻译成中文文本，作为 tool 消息回灌给模型：

| 失败 | 位置 | 回灌给模型的内容 |
|---|---|---|
| 工具不存在 | base.py:107 | `错误：不存在名为 'xxx' 的工具。可用工具：...` |
| 参数不是合法 JSON | base.py:118 | `错误：参数不是合法 JSON（...）。收到的原文：... 请重新生成` |
| 参数不匹配（TypeError） | base.py:136 | `错误：参数不匹配（...）。请对照工具定义修正后重试` |
| 执行时任何异常 | base.py:143 | `错误：执行时发生未预期的异常：XxxError: ...` |

> **理由**：agent 的鲁棒性来源是"模型能看见错误并自己纠正"，而不是"程序不出错"。
> 模型看到"文件不存在：src/mian.py"能自己发现拼写错了；如果异常抛到顶层，
> 整轮任务就断了，用户还得重新描述一遍需求。
>
> 而且错误文本是**写给模型看的**，所以带指导性 —— 不只说"错了"，
> 还说"请重新生成一次格式正确的调用"。

---

### 3.4 七个工具

| 工具 | 位置 | 关键实现 | 需确认 |
|---|---|---|---|
| `read_file` | filesystem.py:47 | 带行号返回，支持 offset/limit 分段；上限 2000 行、每行 500 字符；非 UTF-8 拒绝 | — |
| `list_dir` | filesystem.py:107 | 递归树，默认深度 2 最大 5；跳过 `.git`/`node_modules` 等噪音目录；上限 300 条 | — |
| `grep` | search.py:39 | 纯 Python `re`，不 shell 出去调系统 grep/rg；上限 200 条匹配，跳过 >2MB 文件 | — |
| `write_file` | editing.py:39 | 先生成 diff → 请求确认 → 才落盘；非 UTF-8 拒绝覆盖 | ✔ |
| `edit_file` | editing.py:83 | **唯一字符串替换**；匹配 0 次或 >1 次都报错并说明原因 | ✔ |
| `run_command` | shell.py:73 | 黑名单硬拦截 → 确认 → 执行；返回退出码 + stdout + stderr | ✔ |
| `todo_write` | planning.py:59 | 全量覆盖式清单；强制同时最多一项 `in_progress` | — |

**辩护点：`edit_file` 为什么用唯一字符串匹配，不用行号？**
> 行号会因为上下文陈旧而错位 —— 模型是几步之前读的文件，中间可能被别的调用改过。
> 整段重写又太浪费输出 token、容易在长文件里漏掉不该动的内容。
> 唯一字符串匹配失败时直接说清原因让模型重新 `read_file`，
> **这比"改错地方"更安全 —— 错误是可见可恢复的，静默改错不是。**

**辩护点：`grep` 为什么不调系统 grep / ripgrep？**
> 一是题目要求不依赖服务端托管的执行环境；二是本地有没有 grep/rg 在不同机器上
> 不确定（尤其 Windows）。纯 Python 慢一点，但零依赖、跨平台行为一致。

**辩护点：`todo_write` 为什么值得多加一个工具？**
> 多步任务里模型容易"做着做着忘了还剩什么"，尤其中途被工具报错带偏之后。
> 把计划显式写进对话历史 = 给模型一个随时可查的锚点，同时用户也能看见进度。
> 代价是多一个工具就多一种用错的可能，所以描述里写死"三步以上才用"。
> **强制单项 `in_progress`** 是刻意的：允许多项并行的话，模型会把所有条目一次性
> 标成进行中，清单就退化成一份静态列表，失去跟踪进度的意义。违反时直接报错让它改。

---

### 3.5 上下文管理与压缩 · `history.py` ★ 技术含量最高的部分

这是整个项目最值得讲、也最容易被追问的一块。分三个子问题讲。

#### (1) token 怎么算的？

**不本地跑分词器。** 用 API 每次响应返回的 `usage.prompt_tokens` 作为权威值 —— 它
正好就是"刚刚发出去的这份历史有多大"的精确答案。

```
estimated_tokens() = 上次实测的 prompt_tokens + (之后新增字符数 / 2.5)
```

**辩护点：为什么不用 tiktoken？**
> DeepSeek 没有公开可离线使用的 tokenizer，硬套 tiktoken 在中文上误差能到 30% 以上。
> 而 `usage.prompt_tokens` 是白给的精确值，没有理由不用。只有"上次实测之后新追加的
> 内容"需要本地估算，按 2.5 字符/token 一刀切（中文约 1、英文约 4，取中间值）。
> 这个估算只用于"要不要触发压缩"的**二元判断**，且阈值本身留了余量，不需要更高精度。

**细节**：压缩后必须把实测值清零重估（history.py:166），否则下一轮会拿着压缩前的
旧大数字立刻再次触发压缩。

#### (2) 压缩怎么做的？

触发条件：`estimated_tokens() >= compact_threshold`（默认 40000），
在**发请求之前**检查（loop.py:127）。

流程：
1. 找安全切割点 `cut`
2. `older = messages[:cut]`，`recent = messages[cut:]`
3. 把 `older` 平铺成纯文本，调模型写摘要
4. 用一条 `user` 角色的消息替换掉 `older`

#### (3) ★ 真正的难点：从哪里切

**这是全项目最该讲的一个点。**

OpenAI 兼容协议有一条硬约束：每条 `role="tool"` 的消息，都必须能在它前面找到
一条带有对应 `tool_call_id` 的 assistant 消息。

如果压缩时从中间随便切一刀：

```
[user][assistant+tool_call][tool result][assistant][user]...
                       ↑
                    从这里切
        ↓ 结果
[摘要(user)][tool result][assistant][user]...
              ↑ 孤儿！它引用的 tool_call_id 随 assistant 一起没了
              → 服务端直接 400，而且报错完全不指向压缩逻辑，极难排查
```

`_find_safe_cut()`（history.py:172）的规则：

1. 从 `target = len(messages) - keep_recent` 开始**向后**找
2. **优先切在 `user` 消息处** —— 一轮任务的自然边界，语义最完整
3. 找不到 user 就退而求其次切在**非 tool** 的消息处
4. **整段尾巴都是 tool 消息 → 返回 0，放弃这次压缩**，宁可不压也不切坏

> 「宁可放弃压缩也不切坏」这个取舍要讲出来：压缩失败只是这轮省不下 token，
> 切坏则是整个请求被拒、任务直接断掉。两者代价不对称，所以选保守。

#### (4) 摘要本身的三个设计

| 决策 | 位置 | 理由 |
|---|---|---|
| 摘要用 **user 角色**注入 | history.py:154 | assistant 角色会让模型误以为这些话是它自己说的；对话中段插第二条 system 消息各家兼容实现行为不一致。user 是最稳妥的"外部信息注入"方式 |
| 摘要请求**不带 tools** | loop.py:190 | 带上 tools，模型很可能"接着干活"直接发起新工具调用，而不是老实写摘要。这也是为什么要把对话平铺成纯文本再传 |
| 摘要提示词**面向未来不面向叙事** | prompts.py:40 | 摘要唯一的读者是即将接着干活的模型自己。按「用户目标 / 已完成 / 关键事实 / 未完成待办」结构化输出，要求保留确切的文件路径和标识符名，而不是复述对话过程 |

**失败不能拖垮任务**：摘要调用本身也可能超时或报错，`compact()` 把它包在 try 里
（history.py:144），失败就保持历史原样返回。

#### (5) 压缩开销单独记账

`TurnStats.compaction_tokens` 与 `tokens` 分开统计（loop.py:47）。

> **实测数据（面试可以直接报）**：把阈值强行调到 5000 做测试时，一轮任务干活花了
> 9578 tokens，压缩却额外花了 5051 tokens —— **压缩比省下的还贵**。
> 这说明阈值不能设太低，默认 40000 是在"避免撞上下文上限"和"别为压缩本身付太多钱"
> 之间取的平衡。

---

### 3.6 三层安全边界

#### 第一层：路径沙箱 · `tools/paths.py:49`

```python
target = raw.resolve() if raw.is_absolute() else (ctx.workspace / raw).resolve()
if target != ctx.workspace and ctx.workspace not in target.parents:
    raise ToolError("拒绝访问工作目录之外的路径")
```

先 `.resolve()` 消解 `..` 和符号链接，**再**检查是否落在工作目录内。
顺序不能反 —— 先检查再 resolve 的话 `a/../../etc/passwd` 就绕过去了。

> **为什么放在工具层不放在提示词里？** 提示词是建议，代码才是约束。
> 模型可能读到文件内容里的诱导性指令，也可能自己拼错路径。

#### 第二层：凭据文件拦截 · `tools/paths.py:23`

`.env`、`*.key`、`*.pem`、`id_rsa*`、`.git-credentials`、`.npmrc` 等
**即使位于工作目录内也一律拒绝**；`.env.example` 这类模板放行。

> **这是加 Web 文件预览时发现的真问题**：路径沙箱只管"有没有越出工作目录"，
> 而 `.env` 就躺在工作目录里 —— agent 完全可以 `read_file` 读出自己的 API key
> 并原样打印进对话，而对话会出现在终端录屏和视频里。题目明确要求密钥不得出现在
> 仓库、README 或视频中，这就是个实打实的雷。

**容易漏的一处**（search.py:101）：`grep` 是直接遍历文件读内容的，**没走 `resolve()`**，
所以必须在它的文件迭代器里单独再挡一次 —— 否则一句 `grep sk-` 就能把 key 捞出来。
已验证：拿真实密钥前缀去 grep 搜不到任何东西。

> 讲这段的加分点：**能自己发现并补上这个漏洞，比一开始就没漏更有说服力。**

#### 第三层：权限闸门 · `tools/permissions.py`

| 模式 | 行为 |
|---|---|
| `ask`（默认） | 每次破坏性操作都问，展示 diff 或命令原文；可选"本会话总是允许该工具" |
| `auto` | 跳过确认，但 `run_command` 黑名单仍生效 —— **auto 不等于无底线** |
| `readonly` | 直接拒绝一切写操作，返回值告诉模型"现在只能分析，别再试" |

**两个关键设计**：

1. **fail closed**（permissions.py:67）：没有交互通道时（`asker is None`）一律拒绝，
   不会因为忘记接 UI 就悄悄放行破坏性操作。`Reporter.ask_approval` 的默认实现
   也是返回 `"n"`（loop.py:37）。
2. **确认闸门放在各工具 handler 内部，不在 `dispatch()` 里统一拦**（permissions.py:10 注释）：
   因为不同工具要在确认框里展示的内容不一样（diff vs 命令原文），
   统一拦截只能弹一个内容空洞的框。`requires_approval` 字段目前只做工具自描述用。

#### 高危命令黑名单 · `shell.py:32`

正则硬拦截，**不受权限模式影响**，连确认框都不弹：

`rm -rf` 类 / `del /s /f`、`rd /s` / `format X:` / fork 炸弹 / `git push --force` /
`git reset --hard` / 写磁盘设备 / 关机重启 / `curl ... | sh`

> 这类操作没有"确认后执行"的价值，唯一正确的处理就是拒绝并让模型换一种做法。
> 黑名单**不追求穷尽**（不可能穷尽），只挡最常见、最难挽回的几类 ——
> 属于"防灾难性误操作"，不是"防恶意注入"。

---

### 3.7 会话持久化 · `session.py`

`--resume` 接着上次的对话继续。三个设计决定：

| 决策 | 理由 |
|---|---|
| **不保存 system prompt，恢复时重建** | 系统提示词里嵌着启动那一刻的目录概览。上次会话结束后文件可能已增删，把旧快照恢复回来，模型会"确信"某个文件存在 —— **比没有记忆更糟** |
| **按工作目录分别存档** | 同一个 agent 会在不同项目里用，共用一份历史只会互相污染。存档名 = 目录名 + 绝对路径 SHA1 前 12 位，既可读又不会重名撞车 |
| **原子写入** | 每轮结束都落盘，而落盘时被 Ctrl+C 打断完全可能。先写 `.tmp` 再 `os.replace`，保证存档要么是上一个完整版本、要么是这一个，不会出现写了一半的坏 JSON |

**存档覆盖必须提示**（loop.py:75）—— 这是联调时踩到的坑：
做完一个长任务，随手在同一目录跑个一次性命令（没加 `--resume`），
上次的历史就被覆盖没了，而且毫无提示。现在会明确告知
"该目录有历史会话（N 轮），本次将开新对话并在结束时覆盖它"。
**把一个静默的数据丢失变成一次知情选择。**

**存档调用放在 `finally` 里**（loop.py:113）：三个出口（正常结束 / 步数上限 / 中断报错）
都要落盘，否则 Ctrl+C 一次就丢掉整轮进度。

---

### 3.8 双界面与 Reporter 抽象

`Reporter`（loop.py:26）是一个只有 8 个方法的抽象接口：

```
on_text / on_text_end / on_tool_start / on_tool_end
on_notice / on_error / on_todos / ask_approval
```

两个实现：`ConsoleReporter`（cli.py:24，用 rich）和 `WebReporter`（server.py:71，推 WebSocket 事件）。

> **加浏览器界面时 `agent/` 包一行代码都没改。** 这是抽象设计最直接的验证，
> 面试时可以让评委去 git log 里核对 `feat(web): 浏览器界面，agent 内核零改动` 那次提交的 diff。

**终端界面**：REPL + 7 个斜杠命令（`/help` `/tools` `/clear` `/mode` `/compact` `/resume` `/exit`），
另有 `-p` 单次任务模式、`-C` 指定工作目录、`--mode` 权限模式、`--resume`。

**Web 界面**：文件树 / 文件预览 / 工作目录选择器 / 工具调用卡片 / diff 确认框 /
任务清单卡片 / **上下文占用进度条**。

> **进度条为什么值得做**（DESIGN 第 20 条）：上下文压缩是这个项目技术含量最高的部分，
> 但它平时完全不可见。画一条 `estimated_tokens / compact_threshold` 的进度条后，
> 压缩触发的瞬间它会**肉眼可见地回落** —— 把一个纯后台机制变成了可以直接指着讲的东西。
> **录视频时这一帧很值钱。**

---

### 3.9 Web 端的线程模型 · `server.py`

两个必须处理的问题（这是 Web 部分唯一有技术含量的地方，值得讲）：

**问题一：`Agent.run_turn()` 是同步阻塞的。**
直接在事件循环里跑，整轮任务期间 WebSocket 收不到任何消息 —— 包括用户点"允许"的那条，
**确认框会永久卡死，形成死锁**。所以 agent 跑在独立线程里（server.py:`threading.Thread`）。

**问题二：`ask_approval()` 要在 agent 线程里阻塞等待浏览器的回答。**
用 `threading.Event` 桥接：Reporter 发出 `approval_request` 事件后 `wait()`，
WebSocket 接收侧拿到回答就 `set()` 唤醒。

两个必须补的口子，都已处理：

- **`wait()` 必须带超时**（`APPROVAL_TIMEOUT = 300`）。否则用户直接关掉页面，
  agent 线程会永久挂死。超时按拒绝处理，与 fail-closed 原则一致。
- **WebSocket 断开时在 `finally` 里主动以"拒绝"唤醒**可能还卡着的线程。
  已实测：确认框弹出时粗暴断开连接，文件不会被写出，服务端仍能接受新连接。

**事件跨线程投递**用 `loop.call_soon_threadsafe()`，不能在工作线程里直接 `await ws.send_json()`。

**两类文件接口的边界故意不同**（DESIGN 第 22 条，容易被追问）：

- `/api/tree`、`/api/file` —— **走 agent 同一套路径沙箱**，只能看工作目录内，同样拒绝凭据文件
- `/api/browse` —— **刻意不受沙箱约束**。要在界面上选一个新的工作目录，就必须能看到
  当前工作目录外面，这是功能本身的要求。补偿：只返回目录名、绝不返回文件内容，
  且服务只监听 127.0.0.1。**关键是沙箱本身没被削弱 —— agent 依然只能读写选定的那个目录，
  浏览目录的能力属于人，不属于 agent。**

---

## 四、测试策略

### 4.1 37 个离线测试（`python tests/run_all.py`）

| 文件 | 数量 | 覆盖 |
|---|---|---|
| `test_tools.py` | 14 | 路径沙箱（穿越 / 绝对路径 / 正常放行）、凭据防护（含 grep 不泄露）、权限闸门（readonly / 拒绝 / 总是允许 / fail-closed / 黑名单无视权限模式）、dispatch 兜错、edit_file 唯一匹配 |
| `test_history.py` | 8 | 多轮压缩、**切割点落在 tool 块中间**、**整段尾巴都是 tool 拒绝压缩**、摘要失败保持原样、空摘要保持原样、**连续压缩两次**（摘要被再次摘要）、token 记账、system prompt 幸存 |
| `test_loop.py` | 5 | 三层终止条件、工具报错仍产出结果、未知工具不崩、多个 tool_call 全部有应答 |
| `test_session.py` | 10 | 存档往返、**system prompt 绝不入档**、恢复后 tool 配对完好、工作目录隔离、损坏/版本不符返回 None、todo_write 校验 |

**模型全部打桩**，不联网、不消耗额度。**不引入 pytest** —— 每个测试文件都是可直接执行的
脚本，`run_all.py` 用 subprocess 挨个跑。少一个依赖，评委 clone 下来
`pip install -r requirements.txt` 就能跑。

### 4.2 ★ 压缩保真度评测（`tests/eval_compaction.py`）

**这是最能体现工程思维的一节，强烈建议主动讲。**

"压缩是否正确"其实是三个问题，可验证程度完全不同，混为一谈就答不清楚：

| 层 | 问题 | 能否证明 | 由谁覆盖 |
|---|---|---|---|
| 一 · 结构正确性 | 压缩后仍是合法请求体：无孤儿 tool 消息、每个 tool_call 有结果、system 没丢 | **确定性，可证明** | `test_history.py` |
| 二 · 机械正确性 | 最近若干条原样保留、摘要非空、token 确实下降、失败时保持原样 | **确定性，可证明** | `test_history.py` |
| 三 · 语义保真度 | 摘要有没有保住"继续干活所必需的事实" | **无法证明，只能测量** | `eval_compaction.py` |

**探针测试方法**：在对话里埋下若干可检验的事实（一部分来自用户消息、一部分来自工具返回），
把阈值压到 6000 强制触发压缩，然后**禁用全部工具**提问。

> **禁用工具这一步是关键**：留着工具的话，模型答不上来时会去重新读一遍文件，
> 测出来的就不是"压缩保住了多少"，而是"模型会不会补救"，指标就废了。

**实测结果**：跑两次均为 **6/6 全中**，被考的事实包含压缩前埋入的项目代号与人名
（已确认落在被摘要掉的那一半里，不是靠 `keep_recent` 窗口蒙混过关）。
脚本在召回率低于 80% 时返回非零退出码，可以直接当门禁用。
因为会真实消耗额度，刻意不放进 `run_all.py`。

> **这段话的杀伤力在于**："承认第三层无法证明、只能测量，比假装能证明更诚实。"
> 评委会记住这句。

---

## 五、高频面试问答（准备好这 20 条）

### A 类 · 合规性（必问）

**Q1：你用了 `openai` 这个库，这算不算违反"不得使用 agent 框架/SDK"？**
> 不算。题目允许"模型厂商的 API 客户端库、OpenAI 兼容网关及模型原生的 tool calling 接口"。
> 我只用了 `chat.completions.create` 这一个最底层的 HTTP 接口，它就是个带类型的 HTTP 客户端。
> 我刻意没碰任何"帮你跑循环"的封装 —— 没有 Assistants API、没有 `tool_runner`、
> 没有 Files API、没有 Code Interpreter。题目点名要自己写的五项 —— 对话历史与上下文管理、
> 工具定义与本地执行、模型输出解析、循环终止条件、错误处理 —— 分别在
> `history.py`、`tools/`、`llm.py`、`loop.py`、`base.py` 里，每一项都是独立文件，
> 可以逐个翻。

**Q2：依赖有哪些？**
> 四个：`openai`（HTTP 客户端）、`rich`（终端渲染）、`fastapi` + `uvicorn`（Web 界面，可选）。
> 只用终端界面的话后两个不装也能跑。测试连 pytest 都没引。

### B 类 · 核心机制

**Q3：你的 agent 什么时候停？**
> 见 3.1 三层终止条件。重点讲"不设 finish 工具"的理由。

**Q4：工具执行失败了会怎样？**
> `dispatch()` 永不抛异常，四类失败全部翻译成中文文本回灌给模型。
> 鲁棒性来自"模型能看见错误并自我纠正"，不是"程序不出错"。
> 而且错误文本是写给模型看的，带指导性。

**Q5：如果模型一直调用失败、反复重试呢？**
> 这正是 `max_steps=40` 存在的理由 —— 这种循环不会自己停，而且每一步都在烧钱。
> 撞上限后提示用户，历史保持完整，用户追一句"继续"就能接着干。

**Q6：上下文满了怎么办？（→ 一定会追问切割点）**
> 见 3.5。**这题一定要讲到 tool_call 配对约束和 400 报错**，这是区分"抄来的"和
> "自己写过的"最有效的一个细节。

**Q7：你怎么知道压缩没把信息丢了？**
> 见 4.2 三层验证。这是加分题。

**Q8：token 是怎么算的？**
> 用 API 返回的 `prompt_tokens` 当权威值，只对"上次实测之后新增的内容"按
> 2.5 字符/token 估算。不引入 tiktoken 是因为 DeepSeek 没有公开的离线 tokenizer，
> 硬套在中文上误差能到 30%。而且这个估算只服务于一个二元判断，不需要高精度。

**Q9：流式解析有什么坑？**
> tool_call 按 index 分片累加。见 3.2。

**Q10：为什么工具串行不并行？**
> 写-读依赖。见 3.1 辩护点 ③。

### C 类 · 安全（题目很看重）

**Q11：安全怎么做的？**
> 三层：路径沙箱 → 凭据文件拦截 → 人工确认闸门；外加高危命令黑名单硬拦截。见 3.6。

**Q12：★ 路径沙箱能挡住 `run_command` 吗？**
> **不能，这是设计边界不是疏漏，我实测过。**
>
> 让 agent 读工作目录外的 `C:/Windows/win.ini`：`read_file` 被沙箱正确拒绝，
> 但模型紧接着改用 `run_command` 执行 `Get-Content C:/Windows/win.ini`，**读到了**。
>
> 原因：shell 能做的事没有边界 —— `cd ..`、管道、重定向、调用另一个解释器，
> 靠字符串匹配拦不住。真要隔离需要容器或 OS 级权限控制，超出本项目范围。
>
> 所以 `run_command` 的防线不是沙箱，**是人工确认**。而且这条实测链路正好体现了纵深防御：
> 沙箱挡住文件工具 → 模型试图改用命令绕道 → 确认框弹出并展示命令原文 →
> 用户拒绝 → 模型收到"不要重复同样的调用"的提示。
>
> **auto 模式因此是一次明确的信任让渡** —— 跳过确认就等于放弃这层防线，
> 只剩黑名单兜底。这也是为什么默认模式是 ask 而不是 auto。

> 💡 这题答好了非常加分：主动承认边界 + 有实测证据 + 解释了为什么这个边界是合理的。

**Q13：auto 模式是不是就没有安全性了？**
> 不是。黑名单不受权限模式影响，`rm -rf`、`git push --force`、`git reset --hard`
> 这类即使 auto 也直接拒绝，连确认框都不弹。auto 不等于无底线。

**Q14：怎么保证 API key 不泄露？**
> 三道：`.env` 进 `.gitignore` 从不入库；凭据类文件在工具层一律拒绝读写；
> `grep` 因为绕过了 `resolve()` 所以单独再挡一次。已验证拿真实密钥前缀去 grep 搜不到。

### D 类 · 设计取舍

**Q15：为什么做两套界面？**
> 不是为了炫技，是为了验证抽象。UI 全关在 `Reporter` 接口之后，加 Web 时内核零改动 ——
> 这是可以拿 git diff 证明的。另外 Web 端的上下文进度条让"压缩"这个纯后台机制变得可见。

**Q16：Web 前端为什么不用 React/Vue？**
> agent 必须跑在 Python 进程里（要读写本地文件、执行命令，浏览器碰不了），
> 所以浏览器只能是显示层，页面上统共 5 个元素。npm 那套买的是组件化和打包，
> 这个规模换不回成本，反而给仓库塞进 node_modules、让评委多一步 `npm install`、
> 多一个构建步骤可能在截止前出问题。**单 HTML + 原生 JS，不存在"构建失败"这种状态。**

**Q17：会话恢复为什么不存 system prompt？**
> 它嵌着启动那一刻的目录概览，上次会话后文件可能已增删。恢复旧快照会让模型
> "确信"某个文件存在 —— 比没有记忆更糟。对话内容照旧保留，因为它记录的是
> "做过什么"，这个事实不会过期。

**Q18：`edit_file` 为什么不用行号？**
> 见 3.4。核心是"错误可见可恢复 > 静默改错"。

### E 类 · 反思

**Q19：跟 Claude Code 比差在哪？下一步做什么？**
> 诚实回答，列 3 个：
> 1. **没有子 agent / 任务分解**。复杂任务全在一个上下文里滚，不能像 Claude Code 那样
>    派生子任务隔离上下文。
> 2. **压缩是"整段摘要"**，比较粗。另一种思路是分级处理：老的 tool result 直接丢弃或
>    截断更狠、但保留 user/assistant 原文。前者实现简单压缩率高，后者保真度更好。
>    当前选前者，因为工具结果往往最占体积也最容易过期。
> 3. **run_command 没有真正的沙箱**（见 Q12），要做就得上容器。
>
> 另外只读工具的并发也是明确留的口子（`requires_approval` 字段已预留）。

**Q20：开发过程中最难的是什么？**
> 上下文压缩的切割点。写之前以为主要工作量是设计摘要提示词，实际做下来发现
> 真正的坑是"从哪里切" —— 切坏了服务端返回 400，而且报错信息完全不指向压缩逻辑，
> 排查非常费劲。这件事教会我的是：**协议层的隐式约束比业务逻辑更容易踩，
> 因为它不会在本地报错。** 同类的还有 Windows 上 `shell=True` 实际调的是 cmd.exe
> 而不是 PowerShell —— 也是不报 Python 异常、只让 agent 在特定平台上莫名其妙反复失败。

---

## 六、可能被追问到的细节（提前想好）

**① `keep_recent` 到底是"至少"还是"至多"保留 8 条？**

代码是**至多**。`_find_safe_cut(target)` 从 `target = len - keep_recent` 开始**向后**找，
返回的 `cut >= target`，所以 `recent = messages[cut:]` 的长度 `<= keep_recent`。
如果切割点后移得多（比如 target 后面跟着一长串 tool 消息），实际保留的会少于 8 条。

> 如果被问到：坦率承认"向后找会缩小保留窗口，`config.py` 里那句注释写成'至少'不准确，
> 应该是'至多'"。然后解释这个方向是**刻意的** —— 向后找保证了切割点一定合法且压缩率更高；
> 向前找虽然能保住更多近期消息，但可能找不到合法切点而放弃压缩。
> **结构合法性优先于窗口大小**，与"宁可不压也不切坏"是同一个取舍。

**② 第一次请求前 `_last_prompt_tokens = 0`，估算准吗？**

不准，但**偏保守**（只按字符估，会低估中文）。因为阈值 40000 离真实上下文上限还有很大余量，
低估的后果只是晚一点触发压缩，不会撞上限。

**③ `temperature=0.0` 为什么？**

编程任务要的是确定性和可复现，不是创造性。也让演示更可控。

**④ `max_tool_output=20000` 截断为什么保留头尾不是只留头？**

中间部分通常是重复内容（大量日志、长文件正文），而**头尾包含命令回显和最终报错** ——
直接砍尾部会丢掉最关键的 traceback。

**⑤ 为什么所有文件读写都显式写 `encoding="utf-8"`？**

Python 在中文 Windows 上 `open()` 的默认编码是 GBK，读 UTF-8 源码会直接抛
`UnicodeDecodeError`。

---

## 七、数字速记表

| 项 | 值 | 出处 |
|---|---|---|
| 工具数量 | **7** | `REGISTRY` |
| 离线测试 | **37**（14+8+5+10） | `tests/` |
| 设计决策记录 | **28 条** | `DESIGN.md` |
| 单轮步数上限 `max_steps` | 40 | config.py:46 |
| 压缩阈值 `compact_threshold` | 40000 tokens | config.py:52 |
| 压缩保留最近消息 `keep_recent` | 至多 8 条 | config.py:54 |
| 单条工具结果上限 | 20000 字符 | config.py:48 |
| token 估算比例 | 2.5 字符/token | history.py:125 |
| `temperature` | 0.0 | config.py:43 |
| 请求超时 / 重试 | 120 秒 / 3 次 | config.py:56 |
| `read_file` 上限 | 2000 行、每行 500 字符 | filesystem.py:18 |
| `grep` 上限 | 200 条匹配、跳过 >2MB 文件 | search.py:15 |
| `run_command` 超时 | 默认 60 秒、最大 300 秒 | shell.py:27 |
| Web 确认超时 | 300 秒（超时按拒绝） | server.py:47 |
| 压缩保真度实测 | **6/6**，门禁 80% | eval_compaction.py |
| 压缩成本实测 | 干活 9578 / 压缩 5051 tokens（阈值调到 5000 时） | DESIGN 第 17 条 |

---

## 八、录视频前的自检清单

- [ ] `python demo/setup_demo.py` 重置靶子（**agent 一旦修好 bug 就没得演了，重拍前必跑**）
- [ ] 确认 `.env` 已配好，且**录屏时不要打开 `.env` 或让终端显示 key**
- [ ] 走 A 案例（修 bug，ask 模式）：第 4 步 diff 确认框**停 2–3 秒**，这是全片最有说服力的一帧
- [ ] 如果要展示压缩：盯住 Web 端上下文进度条回落的那一瞬
- [ ] 2 分钟硬上限 —— 建议 **80% 演示 + 20% 讲实现**，讲实现只挑一个点（推荐切割点或凭据泄露）
- [ ] 视频里不要出现仓库地址以外的任何个人信息

---

## 附：现场如果被要求打开代码，按这个顺序翻

1. `agent/loop.py` —— 主循环，30 行看完全貌
2. `agent/llm.py:105` —— `_consume()`，流式分片拼装
3. `agent/history.py:172` —— `_find_safe_cut()`，全项目最该讲的 20 行
4. `agent/tools/base.py:104` —— `dispatch()`，四类兜错
5. `agent/tools/paths.py` —— 两道检查，77 行
6. `DESIGN.md` —— 任何"为什么"都能在这里找到答案
